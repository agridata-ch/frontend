// Delay before auto-redirecting after a consent-request decision is submitted.
export const REDIRECT_TIMEOUT = 4000;

// Router navigation-state flag that tells the consent-requests page to refetch after returning to it.
export const FORCE_RELOAD_CONSENT_REQUESTS_STATE_PARAM = 'refresh';
